type ErrorProps = {
    message?: string;
}

const Error= (e: ErrorProps) => {
    return (
        <div>
            <h1>Nenhu usuário encontrado</h1>
        </div>
    );
};

export default Error;